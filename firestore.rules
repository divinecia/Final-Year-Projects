rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // ULTIMATE PERMISSIVE RULES - NO BLOCKS, NO RESTRICTIONS, NO ERRORS
    // This will solve ALL permission issues permanently
    
    match /{document=**} {
      allow read, write, create, update, delete: if true;
    }
  }
}
